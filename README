Простой преобразователь ссылок
Работает по простому алгоритму, который переводит целочисленный идентификатор в base58 строку
для работы необходимо сделать npm install && bower install
Если необходимо переконфигурировать подключение к БД (используется MongoDB), порт или имя хоста для запуска, то все
это можно сделать в config/config.js